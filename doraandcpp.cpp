#include <iostream>

const int N = 10e5;
int c[N];

int solve() {
    int n, a, b;
    std::cin >> n >> a >> b;
    for (int i = 0; i < n; ++i) {
        int ci;
        std::cin >> ci;
        c[i] = ci;
    }
    int min;
    return min;
}

int main() {
    int t;
    std::cin >> t;
    for (int i = 0; i < t; ++i)
        std::cout << solve() << std::endl;
}
